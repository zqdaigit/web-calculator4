# 冲突同步与双向校验报告 (Issue #WS-31)

## 1. 校验概览 (Overview)

- **校验结论**: FAIL (不通过)
- **工作项**: WS-31 / create / WS-27-simple-web-calculator
- **正式资产**: simple-web-calculator
- **检测时间**: 2026-06-09 11:41:32
- **输入基线**:
  - PRD 文件: `requirements/simple-web-calculator.prd.md`
  - UID 文件: `designs/simple-web-calculator.ux.md`
  - 集成分支: `review/simple-web-calculator`
- **正式资产命名检查**: PASS；未发现以 `WS-31`、`WS-27-simple-web-calculator` 或 `*-bugfix*` 命名的平行 PRD、UX 或原型正式资产。
- **基线检查**: `ChangeType=create`，不适用既有能力回退检查。
- **冲突统计**:
  - CRITICAL: 2 个
  - MAJOR: 7 个
  - MINOR: 0 个

## 2. 双向交叉比对详情 (Cross-Review Details)

### 2.1 正向比对：需求 -> 交互 (PRD -> UID)
> 检查 PRD 中的功能点是否在交互设计中完整体现。

| PRD 功能点定义 | UID 对应表现 | 冲突等级 | 差异与冲突说明 |
| :--- | :--- | :--- | :--- |
| REQ-CALC-001 基础四则运算与链式计算 | 数字、四则运算符、等号按键及连续运算符交互 | PASS | 基础输入与运算入口均有对应设计。 |
| REQ-CALC-002：`C` 仅清除当前输入；`AC` 全部重置；`DEL / Backspace` 删除末位 | 键盘区仅有 `C`，且定义为“清除所有输入”；无 `AC` 与可点击退格按键，仅键盘快捷键支持退格 | CRITICAL | 高优先级控制能力缺失且 `C` 语义直接冲突；触屏用户无法执行 PRD 定义的 AC 与退格操作。 |
| REQ-CALC-003：小数点、百分比、正负号切换 | `.`、`%`、`+/-` 按键及交互 | PASS | 三项辅助运算均有对应设计。 |
| REQ-CALC-004：除零后显示 `Error`，锁定除 `AC` 外所有按键，仅 `AC` 可复位 | 显示详细错误文本，仅置灰运算符按钮，要求点击 `C` 恢复 | CRITICAL | 错误文案、锁定范围和唯一恢复入口均冲突；UID 中不存在 PRD 要求的 `AC`，无法实现规定的异常恢复状态机。 |
| REQ-CALC-004：单次操作数最大长度为 12 位字符 | 数字输入最大长度为 15 位数字 | MAJOR | 输入上限不一致，且“字符”与“数字”的计数口径也未统一。 |
| REQ-CALC-004：高精度纠错，例如 `toFixed(10)`，最多保留 10 位有效小数 | 结果限制为 12 位有效数字 | MAJOR | 精度位数和计量口径不一致，将产生不同展示结果。 |
| REQ-UI-001：手机 `<480px`、平板 `480px-768px`、PC `>768px`；PC 为最大宽度 400px 的居中卡片 | 手机 `320px-480px`、平板 `481px-1024px`、PC `1025px+`；平板/PC 含常驻历史侧栏 | MAJOR | 断点与 PC 布局结构不一致；769px-1024px 区间会被双方归入不同设备布局。 |
| REQ-UI-002：明暗主题切换、300ms 过渡、Hover/Active 动效 | 主题切换、0.3s 过渡、按键 Hover/Active 动效 | PASS | 核心视觉交互要求已覆盖。 |

### 2.2 反向比对：交互 -> 需求 (UID -> PRD)
> 检查交互设计中的页面与组件是否在 PRD 中有业务规则支撑。

| UID 页面/组件/路由 | PRD 规则定义 | 冲突等级 | 差异与冲突说明 |
| :--- | :--- | :--- | :--- |
| 单页计算器主页、数字键、四则运算键、`%`、`+/-`、`.`、`=` | REQ-CALC-001、REQ-CALC-003 | PASS | 核心计算组件有明确需求来源。 |
| 历史记录抽屉/常驻面板、点击记录回填、清空按钮与确认弹窗、空状态 | 仅说明支持“前端内存态或本地会话态的历史查看” | MAJOR | PRD 支持历史查看，但未定义记录写入、记录回填、清空确认、空状态及不同设备形态，交互行为缺少业务规则。 |
| 键盘快捷键映射及 PC 端“快捷键说明”入口 | 无 | MAJOR | PRD 仅定义按键指令，未定义物理键盘映射、快捷键说明组件及其行为。 |
| 点击结果显示区复制到剪贴板并显示 Toast | 无 | MAJOR | 新增了复制能力、剪贴板权限行为和反馈文案，PRD 未定义。 |
| 主题默认跟随系统并记住用户选择 | 仅定义 Light/Dark 一键切换 | MAJOR | 系统主题跟随和选择持久化策略无需求来源，存储范围与优先级未定义。 |
| 溢出时 `>1e15` 使用科学计数法，超过 JavaScript 最大安全整数显示 `Error: Overflow` | 仅概述“除零溢出校验”，无阈值和展示规则 | MAJOR | UID 自行定义阈值、科学计数展示与错误边界，可能形成未确认的业务规则。 |
| 实时双层显示：完整表达式与当前结果 | PRD 定义计算结果在显示区呈现 | PASS | 属于对计算状态与结果的直接展示，未发现与 PRD 明文冲突。 |

## 3. 待整改清单 (Refinement Checklist)

- [ ] **[PM/BA 待办]** 裁决并统一 `C`、`AC`、`DEL / Backspace` 的入口、语义及触屏可达性。
- [ ] **[PM/BA 与交互设计师待办]** 统一除零后的错误文案、锁定范围及唯一恢复入口。
- [ ] **[PM/BA 与交互设计师待办]** 统一输入长度上限、计数口径，以及浮点结果的精度位数与口径。
- [ ] **[PM/BA 与交互设计师待办]** 统一手机、平板、PC 的响应式断点及 PC/平板历史侧栏布局。
- [ ] **[PM/BA 待办]** 明确历史记录的写入、回填、清空、确认、空状态和存储生命周期规则，或要求 UX 移除无源交互。
- [ ] **[PM/BA 待办]** 确认键盘快捷键、快捷键说明、结果复制、Toast、系统主题跟随及主题选择持久化是否纳入范围。
- [ ] **[PM/BA 待办]** 明确溢出阈值、科学计数法展示条件与 `Error: Overflow` 触发规则，或要求 UX 删除未定义规则。

## 4. 局部重试流触发建议 (Refinement Loop Trigger)

当前存在核心控制与异常恢复状态机冲突，建议项目经理先组织 BA 与交互设计师共同裁决 `C / AC / Backspace`、除零恢复、长度/精度及响应式断点；随后由 BA 补齐获准的扩展交互业务规则，或由交互设计师移除无源交互。整改后的正式资产应继续使用稳定路径 `requirements/simple-web-calculator.prd.md` 与 `designs/simple-web-calculator.ux.md`，重新合入 `review/simple-web-calculator` 后再次触发 Sync 校验。
